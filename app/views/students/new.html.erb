<div class='container form'>
  <h2 class='student-form'>Create student profile</h2>
  <br/>
  <p> <%= link_to 'Students', students_path %></p>
  <%= form_for @student, url: { action: :create } do |f| %>
    <%= render partial: 'forms', locals: { f: f, save_prompt: 'Create User' } %>
  <% end %>
</div>
